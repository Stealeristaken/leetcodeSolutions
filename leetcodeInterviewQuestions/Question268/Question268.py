class Solution:
    def missingNumber(self, nums: List[int]) -> int:
        ideal = [i for i in range (0:i
                                   len(nums)+1)]
        nums=list(set(ideal)-set(nums))



        with open("user.out", "w") as file:
    for i in stdin:
        i = list(map(int, i.rstrip()[1:-1].split(',')))
        n = len(i)
        file.write(str(sum(range(0,n+1))-sum(i))+"\n")
exit(0)
